# Add Data in Web-App using Redux.

## Import in Add Data File.

**Step - 1 : Make Functionalities for Insert Data in API**

    > Firstly make 
    
useEffect(() => {
    if(!localStorage.getItem.('user_id'))
    {
        redirect('/);
        return false;
    }
}, []);





